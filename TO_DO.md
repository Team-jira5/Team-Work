## 💻 Step 1: Clone the Repository

Clone the repository locally using:

```bash
git clone https://https://github.com/Team-jira5/Team-Work.git
cd Team-Work


🌿 Step 2: Create a New Feature Branch

Always create a new branch based on the develop branch to work on a feature:

git checkout develop
git pull origin develop
git checkout -b feature/your-feature-name

    Naming should follow the format: feature/feature-name

✍️ Step 3: Make Changes and Commit

After making your code changes, stage and commit them:

git add .
git commit -m "feat: add your feature description"


📤 Step 4: Push Your Feature Branch

Push your feature branch to the remote repository:

git push origin feature/your-feature-name


